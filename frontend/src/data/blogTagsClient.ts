import {API_URL} from "../utils/constants";
import axios from 'axios';

const RESOURCE_URL = API_URL + "/blog-tags";

class BlogTagsClient {
    static getAll(page: number, size: number) {

    }

    static getByTag(blogTagName: string) {

    }
}